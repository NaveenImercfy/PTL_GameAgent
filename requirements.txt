# Core ADK dependency
google-adk>=1.22.0

# Load .env files
python-dotenv

# Web framework + ASGI server
fastapi
uvicorn[standard]

# HTTP client (used by /chat endpoint to call /run internally)
httpx

# SSE streaming support (used by ADK)
sse-starlette

# Firestore for multi-instance session persistence (Cloud Run)
google-cloud-firestore
